<main class="container">
  <%= link_to sanitize("&laar; Back to quote"), quotes_path(@quote) %>

  <div class="header">
    <h1>Edit date</h1>
  </div>
  <%= render "form", quote: @quote, line_item_date: @line_item_date %>

</main>
